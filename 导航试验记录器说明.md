# 人工导航试验后台记录器

运行导航系统后，另开一个终端启动记录器；它不会发送目标点或控制机器人。完成一条人工导航轨迹后按 `Ctrl+C`，该次试验会写入 `~/auto_nav2_eval/<run_name>/`。

## 三组导航试验指令

每一组都使用两个终端。先在两个终端分别执行：

```bash
cd /home/xu/project/auto_nav2
source /opt/ros/humble/setup.bash
source install/setup.bash
export ROS_LOCALHOST_ONLY=1
```

三组均启用 IMU 航向约束；它们只比较 ORB 局部修正及其筛选融合策略。每次导航完成后，在终端 B 按 `Ctrl+C` 生成该次的 `summary.json`。

默认 `orb_min_f1_score:=101.0`，高于 F1 的理论上限 100，因此 ORB 不会因达到 F1 阈值提前停止，而是在 `orb_max_iterations` 的候选上限内完成搜索。

### 1. 里程计 + IMU 基线

终端 A：

```bash
ros2 launch rtabmap_localization_bringup gazebo_nav_xfeat_odometry.launch.py \
  start_orb_matcher:=false \
  use_imu_yaw:=true \
  start_pose_logger:=false
```

终端 B：

```bash
ros2 run nav_slam nav_experiment_logger --ros-args \
  -p use_sim_time:=true \
  -p experiment_group:=odom_imu \
  -p run_name:=odom_imu_01
```

### 2. ORB + IMU，不进行校正量筛选

终端 A：

```bash
ros2 launch rtabmap_localization_bringup gazebo_nav_xfeat_odometry.launch.py \
  start_orb_matcher:=true \
  use_imu_yaw:=true \
  max_delta_translation_diff_m:=999.0 \
  max_delta_yaw_diff_deg:=180.0 \
  start_pose_logger:=false
```

终端 B：

```bash
ros2 run nav_slam nav_experiment_logger --ros-args \
  -p use_sim_time:=true \
  -p experiment_group:=orb_imu \
  -p run_name:=orb_imu_01
```

### 3. ORB + IMU + 校正量筛选融合

终端 A：

```bash
ros2 launch rtabmap_localization_bringup gazebo_nav_xfeat_odometry.launch.py \
  start_orb_matcher:=true \
  use_imu_yaw:=true \
  max_delta_translation_diff_m:=0.50 \
  max_delta_yaw_diff_deg:=45.0 \
  start_pose_logger:=false
```

终端 B：

```bash
ros2 run nav_slam nav_experiment_logger --ros-args \
  -p use_sim_time:=true \
  -p experiment_group:=orb_imu_fusion \
  -p run_name:=orb_imu_fusion_01
```

`max_delta_translation_diff_m` 和 `max_delta_yaw_diff_deg` 分别限制单次 ORB 局部校正的平移与航向大小。第二组的 `999.0 / 180.0` 基本等于不筛选；第三组的 `0.50 m / 45°` 只接受幅度较小的校正。时延补偿启用后，校正会回写到激光扫描时刻并重放至当前时刻。

每组应重复多次；同一组的 `experiment_group` 必须保持一致，并使用递增的 `run_name`，例如 `orb_imu_fusion_02`。每次结束后会生成：

- `trajectory.csv`：Gazebo `fishbot` 真值、`/odom_in_map` 估计、对齐后的轨迹及逐点误差；
- `orb_events.csv`：每次 ORB 匹配的结果、耗时和是否超过 5 秒；
- `fusion_events.csv`：融合节点的 `fused` / `rejected` 判定；
- `summary.json`：ATE、RPE、终点误差、有效修正率、拒绝率、超时率和平均 ORB 耗时；
- `~/auto_nav2_eval/all_runs_summary.csv`：所有已结束试验的汇总表。
- `~/auto_nav2_eval/group_summary.csv`：按 `experiment_group` 聚合后的均值与总次数，可用于表 2。

默认真值来自 `/gazebo/model_states` 中名为 `fishbot` 的模型。估计轨迹采用自动模式：优先 `/odom_in_map`，两秒内没有该话题时自动使用 `/odom`，因此可直接用于 `gazebo_nav_web.launch.py` 的基线试验。记录器默认在首帧把估计轨迹刚体对齐到真值，因此 ATE/RPE 衡量持续导航的漂移；若要统计绝对 map/world 误差，追加 `-p align_initial_pose:=false`。实机没有 Gazebo 真值时，应替换 `ground_truth_topic` 并改用发布 `gazebo_msgs/ModelStates` 兼容真值的桥接节点，或另行接入外部定位真值。
