# 持续 ORB 三组导航对比实验

正式实验由导航系统、记录器和固定目标序列节点三个进程组成。记录器等待首次全局 ORB 已写入 `map→odom` 后才开始首帧 SE(2) 对齐；目标序列全部到达或某个目标超时后自动结束。结果写入 `~/auto_nav2_eval/<run_name>/`。

## 构建与单元测试

```bash
cd /home/xu/project/auto_nav2
source /opt/ros/humble/setup.bash
colcon build --packages-select nav_slam rtabmap_localization_bringup --symlink-install
source install/setup.bash
python3 -m pytest -q src/nav_slam/test/test_nav_experiment_logger_math.py
```

应显示 `16 passed`，并且 `ros2 pkg executables nav_slam | grep nav_experiment` 应同时列出记录器和目标序列节点。

## 推荐：一条命令完成一组实验

构建并 `source install/setup.bash` 后，依次执行下面三条命令。每条命令都会启动导航、记录器和同一目标序列，实验完成后自动关闭整套 launch。

```bash
# 第1轮：轮式里程计 + IMU
ros2 launch rtabmap_localization_bringup \
  gazebo_continuous_orb_experiment.launch.py \
  experiment_mode:=odom_imu experiment_seed:=10001 run_name:=odom_imu_01 \
  output_dir:=/home/xu/auto_nav2_eval/orb_drift_fixed_20260819 \
  start_nav_rviz:=true

# 第1轮：单观测持续 ORB
ros2 launch rtabmap_localization_bringup \
  gazebo_continuous_orb_experiment.launch.py \
  experiment_mode:=orb_single_observation experiment_seed:=10001 \
  run_name:=orb_single_observation_01 \
  output_dir:=/home/xu/auto_nav2_eval/orb_drift_fixed_20260819 \
  start_nav_rviz:=true

# 第1轮：完整方法（复合一致性总门控）
ros2 launch rtabmap_localization_bringup \
  gazebo_continuous_orb_experiment.launch.py \
  experiment_mode:=orb_quality_gated experiment_seed:=10001 \
  run_name:=orb_quality_gated_01 \
  output_dir:=/home/xu/auto_nav2_eval/orb_drift_fixed_20260819 \
  start_nav_rviz:=true
```

本文将两级判定统一定义为“复合一致性门控”：首先检查候选 `map→odom` 相对当前可信校正目标的创新一致性，阈值为 `0.50m/5°`；对于首次出现或相对稳定跟踪结果发生突变的候选，再检查相邻双观测之间的一致性，阈值为 `0.30m/5°`。两级条件共同构成一个总门控，而不是两个独立实验模块。基线组和单观测组自动将该总门控关闭，不改变原有对照逻辑。

如果前两组已经用相同代码、种子、目标序列和传感器配置完成，而本次修改只影响完整方法的总门控，可以仅重测第三组。为避免旧的异常第三组进入自动聚合，使用新的输出目录：

```bash
ros2 launch rtabmap_localization_bringup \
  gazebo_continuous_orb_experiment.launch.py \
  experiment_mode:=orb_quality_gated experiment_seed:=10001 \
  run_name:=orb_quality_gated_01 \
  output_dir:=/home/xu/auto_nav2_eval/orb_drift_innovation_20260819 \
  start_nav_rviz:=true
```

论文中的第三组统一表述为“完整方法（复合一致性门控）”，组 2 与组 3 的差异即为是否启用该总门控，其性能差异用于评价总门控的整体有效性。

第2轮将三条命令中的种子统一改为 `10002`，运行名后缀改为 `_02`；后续轮次同理。建议正式论文至少重复 10 轮。

## 手动调试：三终端启动

需要分别观察各节点时可使用下面的三终端方式。三组都设置 `goal_relocalization_enabled:=false`，保证目标之间不会插入额外全局重定位；启动阶段的首次全局 ORB 定位仍然保留。相同轮次必须使用相同 `experiment_seed` 和完全相同的 `goals` 字符串。

下面的示例目标均位于当前 `dashgo_slam_map` 最大连通自由区域，格式为 `x,y,yaw_deg`：

```text
7.85,-4.06,0; -8.60,-3.31,180; 8.50,2.34,90; 4.00,-1.16,-157
```

### 1. 轮式 + IMU 基线（无持续 ORB）

终端 A：

```bash
cd /home/xu/project/auto_nav2
source /opt/ros/humble/setup.bash
source install/setup.bash
export ROS_LOCALHOST_ONLY=1

ros2 launch rtabmap_localization_bringup gazebo_nav_xfeat_odometry_experiment.launch.py \
  experiment_seed:=10001 \
  start_orb_matcher:=false \
  goal_relocalization_enabled:=false
```

终端 B：

```bash
cd /home/xu/project/auto_nav2
source /opt/ros/humble/setup.bash
source install/setup.bash
export ROS_LOCALHOST_ONLY=1

ros2 run nav_slam nav_experiment_logger --ros-args \
  -p use_sim_time:=true \
  -p output_dir:=/home/xu/auto_nav2_eval/orb_drift_fixed_20260819 \
  -p estimate_topic:=/odom_in_map \
  -p experiment_group:=odom_imu \
  -p run_name:=odom_imu_01 \
  -p stop_on_goal_reached:=false \
  -p experiment_seed:=10001 \
  -p continuous_orb_enabled:=false \
  -p orb_required_consistent_matches:=0
```

终端 C：

```bash
ros2 run nav_slam nav_experiment_goal_runner --ros-args \
  -p use_sim_time:=true \
  -p goals:="7.85,-4.06,0; -8.60,-3.31,180; 8.50,2.34,90; 4.00,-1.16,-157"
```

### 2. 单观测持续 ORB（不做一致性确认）

终端 A：

```bash
ros2 launch rtabmap_localization_bringup gazebo_nav_xfeat_odometry_experiment.launch.py \
  experiment_seed:=10001 \
  goal_relocalization_enabled:=false \
  orb_required_consistent_matches:=1
```

终端 B：

```bash
ros2 run nav_slam nav_experiment_logger --ros-args \
  -p use_sim_time:=true \
  -p output_dir:=/home/xu/auto_nav2_eval/orb_drift_fixed_20260819 \
  -p estimate_topic:=/odom_in_map \
  -p experiment_group:=orb_single_observation \
  -p run_name:=orb_single_observation_01 \
  -p stop_on_goal_reached:=false \
  -p experiment_seed:=10001 \
  -p continuous_orb_enabled:=true \
  -p orb_required_consistent_matches:=1
```

终端 C 使用与基线组完全相同的 `nav_experiment_goal_runner` 命令。

### 3. 完整质量门控方案（推荐）

终端 A：

```bash
ros2 launch rtabmap_localization_bringup gazebo_nav_xfeat_odometry_experiment.launch.py \
  experiment_seed:=10001 \
  goal_relocalization_enabled:=false \
  orb_max_tracking_innovation_translation_m:=0.50 \
  orb_max_tracking_innovation_yaw_deg:=5.0
```

终端 B：

```bash
ros2 run nav_slam nav_experiment_logger --ros-args \
  -p use_sim_time:=true \
  -p output_dir:=/home/xu/auto_nav2_eval/orb_drift_fixed_20260819 \
  -p estimate_topic:=/odom_in_map \
  -p experiment_group:=orb_quality_gated \
  -p run_name:=orb_quality_gated_01 \
  -p stop_on_goal_reached:=false \
  -p experiment_seed:=10001 \
  -p continuous_orb_enabled:=true \
  -p orb_required_consistent_matches:=2 \
  -p orb_max_tracking_innovation_translation_m:=0.50 \
  -p orb_max_tracking_innovation_yaw_deg:=5.0
```

终端 C 使用与基线组完全相同的 `nav_experiment_goal_runner` 命令。

同一批正式数据使用一个全新的 `output_dir`，避免历史试跑进入聚合结果。同一试验组保持相同的 `experiment_group`，每次使用递增的 `run_name`。第 2 轮三组都将种子改为 `10002`，同时将运行名后缀改为 `_02`。正式实验固定使用 `/odom_in_map`，不允许静默回退到 `/odom`。

## 输出文件

- `trajectory.csv`：Gazebo 真值、估计与首帧对齐轨迹，以及位置误差、有符号/绝对航向误差、综合位姿误差和逐点联合精度判定。
- `orb_events.csv`：每次持续 ORB 的序号、匹配状态、F1、耗时和超时信息。
- `correction_events.csv`：`global_applied`、`held`、`accepted`、`innovation_rejected` 事件；其中 `tracking_*_innovation` 是候选相对当前可信目标的门控量。
- `summary.json`：ATE、1 s SE(2) RPE、位置/航向 P95、综合误差 RMSE/P95、联合精度满足率、最大校正增量、同步质量、结束原因、实验参数和 ORB 统计。
- `~/auto_nav2_eval/all_runs_summary.csv`：所有已结束试验的汇总；新旧试验字段会合并保留。
- `~/auto_nav2_eval/group_summary.csv`：按 `experiment_group` 输出各指标的均值和样本标准差；只有 `paper_statistics_eligible=true` 的完整运行参与计算，失败/手动中止运行只计入 `run_count_total`。

`orb_correction_acceptance_rate_pct` 的分母是成功 ORB 匹配数，而不是全部里程计消息。它衡量的是持续 ORB 匹配结果被 `map_odom_corrector` 接受的比例。

## 试验口径

默认 `align_initial_pose:=true`，并采用 `position_tolerance_m:=0.50`、`yaw_tolerance_deg:=5.0`。综合误差为：

```text
sqrt(0.5 * ((position_error / 0.50)^2 + (abs(yaw_error) / 5.0)^2))
```

联合满足率要求位置误差不超过 `0.50m` 且绝对航向误差不超过 `5°`。要测绝对 map/world 误差，请追加：

```bash
-p align_initial_pose:=false
```

关闭后不再进行航向或平移对齐，输出即为原始 map/world 绝对误差。

组 1 与组 2 用于验证持续 ORB 的作用，组 2 与组 3 用于验证复合一致性总门控的作用，组 1 与组 3 表示完整方案的综合增益。只有 `finished_reason=goal_sequence_completed`、`initial_global_correction_observed=true` 且三组 `sample_count`/导航时长接近的运行才能进入论文统计。

## ORB、AMCL 与全局 ICP 初始定位对比

三项测试只启动 Gazebo、传感器和对应的初始定位节点，不启动路径规划及后续导航。ORB 使用当前导航方案中的 `lidar_global_localize` 完成一次全局匹配；AMCL 在初始定位期间控制机器人原地旋转以使粒子滤波收敛；ICP 使用单帧激光进行全图粗搜索和点到点精配准。

### 构建

```bash
cd /home/xu/project/auto_nav2
source /opt/ros/humble/setup.bash
colcon build --packages-select nav_slam rtabmap_localization_bringup
source install/setup.bash
```

### 查看100组测试计划

```bash
python3 test_initial_localization_baselines.py \
  --method orb --runs 100 --seed-base 10001 --dry-run

python3 test_initial_localization_baselines.py \
  --method amcl --runs 100 --seed-base 10001 --dry-run

python3 test_initial_localization_baselines.py \
  --method icp --runs 100 --seed-base 10001 --dry-run
```

### 单组冒烟测试

正式运行前建议各执行一组。`--verbose` 会显示完整的 Gazebo 和定位日志。

```bash
python3 test_initial_localization_baselines.py \
  --method orb --runs 1 --timeout 180 \
  --seed-base 10001 --fast-cleanup --verbose

python3 test_initial_localization_baselines.py \
  --method amcl --runs 1 --timeout 120 \
  --seed-base 10001 --fast-cleanup --verbose

python3 test_initial_localization_baselines.py \
  --method icp --runs 1 --timeout 120 \
  --seed-base 10001 --fast-cleanup --verbose
```

### 正式运行100组当前 ORB 全局定位

```bash
python3 test_initial_localization_baselines.py \
  --method orb --runs 100 --timeout 180 \
  --seed-base 10001 --fast-cleanup
```

### 正式运行100组 AMCL

```bash
python3 test_initial_localization_baselines.py \
  --method amcl --runs 100 --timeout 120 \
  --seed-base 10001 --fast-cleanup
```

### 正式运行100组全局 ICP

```bash
python3 test_initial_localization_baselines.py \
  --method icp --runs 100 --timeout 120 \
  --seed-base 10001 --fast-cleanup
```

三种方法使用相同的 `seed-base` 和运行编号，因此 ORB、AMCL、ICP 的第 `i` 组具有相同出生位姿。默认正确定位判据为平移误差不超过 `0.50m` 且航向误差不超过 `5°`，可用 `--success-xy-m` 和 `--success-yaw-deg` 修改。论文中的配对比较应使用这里生成的新 ORB 数据；旧的 `test_orb_localization.py` 数据没有固定种子和统一统计字段，不与本批 AMCL/ICP 数据混用。

结果默认写入 `test_results/`：

- `orb_results_<时间戳>.csv` / `amcl_results_<时间戳>.csv` / `icp_results_<时间戳>.csv`：逐组出生位姿、估计位姿、误差、耗时和状态。
- `orb_summary_<时间戳>.json` / `amcl_summary_<时间戳>.json` / `icp_summary_<时间戳>.json`：方法输出率、正确定位成功率及误差/耗时统计。
- `<方法>_debug_run_*.log`：未达到正确定位判据时保存的完整日志。

`method_success` 表示算法自身报告收敛或匹配成功；`accuracy_success` 才表示结果同时通过 Gazebo 真值的 `0.50m/5°` 判据。论文中的定位成功率应使用 `accuracy_success`。
