# ORB 全局重定位加速模块对比结果整理

## 1. 分析目的

本次消融试验不再用总耗时同时评价“最小距离范围筛选”和“距离变换跳跃扫描”，而是根据两个模块各自的作用分别设置评价指标：

1. **最小距离范围筛选**：比较原始方法（baseline）与仅筛选方法（filter），评价候选空间压缩率、计划迭代次数和正确定位成功率。
2. **距离变换跳跃扫描**：比较仅筛选方法（filter）与筛选加跳跃扫描方法（full），评价单候选模拟扫描耗时。

总耗时、累计模拟扫描耗时和累计 ORB 匹配耗时可作为补充结果，但不作为两个模块加速能力的核心评价指标。原因是程序具有早停和固定软时间预算，不同方法实际测试的候选点数量不同，累计耗时同时受到“单候选处理速度”和“已处理候选数量”的影响。

## 2. 试验数据与统计口径

- 地图：`dashgo_slam_map.yaml`
- 仿真世界：`small_house.world`
- 每组试验次数：100 次，共 300 条结果
- 三组方法：原始方法（baseline）、仅最小距离筛选（filter）、筛选加跳跃扫描（full）
- 最大迭代次数：250
- 软时间预算：5000 ms
- F1 早停阈值：50
- 正确定位判据：位置误差不大于 0.5 m，且航向误差不大于 5°
- 统计量：候选数、计划迭代次数和单候选耗时采用 100 次试验的中位数；成功率采用正确定位次数除以总试验次数。

数据文件：[`all_trials.csv`](test_results/orb_acceleration_ablation_20260821_125716/all_trials.csv) 和 [`group_summary.csv`](test_results/orb_acceleration_ablation_20260821_125716/group_summary.csv)。

## 3. 方向一：评价最小距离范围筛选

### 3.1 对比对象

- 对照组：原始方法（baseline），不筛选、不跳跃。
- 试验组：仅最小距离筛选（filter），进行筛选、不使用跳跃扫描。

这一对比只改变候选区域筛选策略，因此可用于判断筛选模块是否有效缩小搜索空间，以及是否在保持定位成功率的同时减少后续计划搜索量。

### 3.2 指标与公式

对第 \(i\) 次配对试验，候选空间压缩率定义为：

$$
r_{\mathrm{candidate},i}
=1-\frac{N_{\mathrm{filter},i}}{N_{\mathrm{baseline},i}},
$$

其中，\(N_{\mathrm{baseline},i}\) 和 \(N_{\mathrm{filter},i}\) 分别表示原始方法和仅筛选方法的候选点数。汇总时报告 100 次配对试验压缩率的中位数：

$$
R_{\mathrm{candidate}}
=\operatorname{median}_{i}\left(r_{\mathrm{candidate},i}\right).
$$

按本批数据的组中位数计算：

$$
R_{\mathrm{candidate}}
=1-\frac{6912.5}{80131}
=91.4\%.
$$

计划迭代次数减少率定义为：

$$
R_{\mathrm{iteration}}
=1-\frac{\widetilde I_{\mathrm{filter}}}
{\widetilde I_{\mathrm{baseline}}},
$$

其中，\(\widetilde I\) 表示计划迭代次数的组中位数。本批数据为：

$$
R_{\mathrm{iteration}}
=1-\frac{41}{100}
=59.0\%.
$$

正确定位成功率定义为：

$$
P_{\mathrm{success}}
=\frac{N_{\mathrm{success}}}{N_{\mathrm{trial}}}\times100\%.
$$

### 3.3 表 1：最小距离范围筛选效果

| 评价指标 | 原始方法 baseline | 仅筛选 filter | 对比结果 |
|---|---:|---:|---:|
| 候选点数中位数 | 80,131 | 6,912.5 | 候选空间压缩 91.4% |
| 计划迭代次数中位数 | 100 | 41 | 减少 59.0% |
| 正确定位成功率 | 93% | 97% | 提高 4 个百分点 |

表注：成功率由 93% 提高至 97%，但本批配对试验中该差异不能表述为具有统计显著性。因此，成功率结果用于说明筛选后定位性能没有下降，不用于声称筛选模块显著提高了成功率。

### 3.4 图 1 应画什么

图题建议：**最小距离范围筛选对候选搜索空间和计划迭代次数的影响**。

采用左右两个子图：

- 子图（a）：baseline 与 filter 的候选点数分布。横轴为方法，纵轴为候选点数；使用箱线图并叠加每次试验的散点，中位数标注为 80,131 和 6,912.5。由于两组数量级差异较大，纵轴建议使用对数刻度，并在图中标注“候选空间压缩 91.4%”。
- 子图（b）：baseline 与 filter 的计划迭代次数分布。横轴为方法，纵轴为计划迭代次数；使用箱线图并叠加散点，中位数标注为 100 和 41，并标注“减少 59.0%”。可在两组上方补充正确定位成功率 93% 和 97%。

图注应说明：每组包含 100 次试验；箱体表示四分位区间，中线表示中位数，散点表示单次试验；正确定位判据为位置误差不大于 0.5 m 且航向误差不大于 5°。

### 3.5 可直接用于论文的结果表述

与原始方法相比，最小距离范围筛选将候选点数中位数由 80,131 降低至 6,912.5，对应候选空间压缩率为 91.4%；计划迭代次数中位数由 100 降低至 41，减少 59.0%。与此同时，正确定位成功率由 93% 提高至 97%。结果表明，该筛选策略能够在不降低定位成功率的前提下明显缩小候选搜索空间，并减少后续计划搜索量。成功率提高 4 个百分点仅作为描述性结果，不作统计显著性结论。

## 4. 方向二：评价距离变换跳跃扫描

### 4.1 对比对象

- 对照组：仅最小距离筛选（filter），筛选后逐像素进行模拟扫描。
- 试验组：筛选加跳跃扫描（full），在相同筛选候选区域上使用距离变换跳跃步进。

filter 与 full 在每次配对试验中使用相同的筛选阈值和相同的筛选候选集合。因此，两组单候选模拟扫描耗时的差异可用于评价跳跃扫描本身的加速效果。

### 4.2 指标与公式

首先在每次试验内部计算单候选模拟扫描耗时：

$$
t^{\mathrm{scan}}_{g,i}
=\frac{T^{\mathrm{sim}}_{g,i}}{N^{\mathrm{tested}}_{g,i}},
\qquad g\in\{\mathrm{filter},\mathrm{full}\},
$$

其中，\(T^{\mathrm{sim}}_{g,i}\) 为第 \(i\) 次试验的累计模拟扫描耗时，\(N^{\mathrm{tested}}_{g,i}\) 为该次试验实际测试的候选点数。随后分别取两组单候选耗时的中位数，并计算跳跃扫描加速比：

$$
S_{\mathrm{scan}}
=\frac{\operatorname{median}_{i}
\left(t^{\mathrm{scan}}_{\mathrm{filter},i}\right)}
{\operatorname{median}_{i}
\left(t^{\mathrm{scan}}_{\mathrm{full},i}\right)}.
$$

本批数据为：

$$
S_{\mathrm{scan}}
=\frac{283.5}{68.6}
=4.13.
$$

对应的单候选扫描耗时下降率为：

$$
R_{\mathrm{scan}}
=1-\frac{68.6}{283.5}
=75.8\%.
$$

注意：应先对每次试验计算“累计模拟扫描耗时 ÷ 实际测试候选点数”，再对 100 个单次结果取中位数，不能直接用累计总耗时除以筛选后的全部候选点数。

### 4.3 表 2：距离变换跳跃扫描效果

| 评价指标 | 仅筛选 filter | 筛选加跳跃 full | 对比结果 |
|---|---:|---:|---:|
| 单候选模拟扫描耗时中位数 | 283.5 ms | 68.6 ms | 降低 75.8% |
| 模拟扫描速度（以 filter 为基准） | 1.00 倍 | 4.13 倍 | 加速 4.13 倍 |
| 单候选 ORB 匹配耗时中位数 | 44.8 ms | 43.2 ms | 基本不变 |
| 正确定位成功率 | 97% | 100% | 提高 3 个百分点 |

表注：filter 与 full 具有相同的筛选候选集合和相同的筛选阈值。单候选耗时均在每次试验内部计算后再取组中位数。ORB 单候选匹配耗时基本不变，说明主要加速来源是模拟扫描生成模块，而不是 ORB 匹配模块。

### 4.4 图 2 应画什么

图题建议：**距离变换跳跃步进对单候选模拟扫描耗时的影响**。

横轴设置为 filter 和 full，纵轴设置为单候选模拟扫描耗时（ms/候选点）。使用箱线图并叠加 100 次试验的散点，分别标注两组中位数 283.5 ms 和 68.6 ms；在两组之间标注“4.13 倍加速”或“耗时降低 75.8%”。纵轴可使用线性刻度；若少量长尾值压缩了箱体显示，再改用对数刻度。

图注应明确单候选模拟扫描耗时的计算方式为 \(T^{\mathrm{sim}}/N^{\mathrm{tested}}\)，并说明 filter 与 full 使用相同的候选筛选结果，以保证该图反映的是跳跃扫描模块差异。

### 4.5 可直接用于论文的结果表述

在候选筛选区域及筛选阈值相同的条件下，距离变换跳跃步进将单候选模拟扫描耗时中位数由 283.5 ms 降低至 68.6 ms，耗时下降 75.8%，对应 4.13 倍扫描加速。两组单候选 ORB 匹配耗时分别为 44.8 ms 和 43.2 ms，基本保持不变，说明性能提升主要来自模拟扫描生成模块，而非 ORB 匹配过程。full 组正确定位成功率为 100%，表明引入跳跃扫描后没有观察到定位成功率下降。

## 5. 总耗时结果的使用方式

总耗时不宜作为本次模块消融的主要结论。筛选策略会改变计划迭代次数，跳跃扫描加快单候选处理后又可能使程序在相同时间预算内测试更多候选点，因此总耗时不只反映算法单步速度。

若论文需要保留总耗时，可将其放在补充表或现象分析中，并同时报告实际测试候选点数、累计模拟扫描耗时和累计 ORB 匹配耗时。正文的核心结论应保持为：

1. 距离筛选通过压缩候选空间、减少计划迭代次数发挥作用。
2. 跳跃扫描通过降低单候选模拟扫描耗时发挥作用。
3. 两个模块的成功率均未出现下降。

## 6. 推荐的论文图表顺序

1. 图 1：最小距离范围筛选对候选点数和计划迭代次数的影响。
2. 表 1：候选空间压缩率、计划迭代次数减少率和成功率汇总。
3. 图 2：filter 与 full 的单候选模拟扫描耗时对比。
4. 表 2：跳跃扫描耗时下降率、加速比、单候选 ORB 耗时和成功率汇总。

这种组织方式使“搜索空间优化”和“单候选扫描加速”分别由对应指标支撑，避免使用总耗时混合解释两个不同模块的作用。
